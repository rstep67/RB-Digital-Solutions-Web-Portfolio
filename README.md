Setup:
1. Clone repository into XAMPP htdocs folder
2. Run `composer install` to install dependencies for mail testing (necessary for new_client_controller.php /autoload.php)
3. Import database schema
4. Start `maildev`
5. Run Apache and MySQL via XAMPP
