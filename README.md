Setup:
1. Clone repository into XAMPP htdocs folder
2. Enable the MySQL event listener (event_scheduler=ON in my.ini) 
3. Run `composer install` to install dependencies for mail testing (necessary for new_client_controller.php /autoload.php)
4. Import database schema
5. Start `maildev`
6. Run Apache and MySQL via XAMPP

